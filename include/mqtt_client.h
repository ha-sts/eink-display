#ifndef MQTT_CLIENT_H
#define MQTT_CLIENT_H

#include <cstdint>
#include <vector>
#include <string>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

class mqtt_client
{
    public:
        mqtt_client();

    protected:

    private:
};

#endif // MQTT_CLIENT_H
